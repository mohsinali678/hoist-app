import React from 'react';
import {
    LineChart,
    Line,
    CartesianGrid,
    Tooltip,
    Legend,
    ResponsiveContainer,
    XAxis,
    YAxis
} from 'recharts';
import { useParams } from 'react-router-dom';

const StatView = () => {
    return (
        <ResponsiveContainer>
            
        </ResponsiveContainer>
    )
}

export default StatView;
